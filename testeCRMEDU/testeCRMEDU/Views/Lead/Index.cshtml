@model List<Lead>
@{
}

<h2 class="text-center mb-5">Lista de Leads</h2>

<div class="row mb-5">
    <div class="col">
        <a class="btn btn-primary" asp-controller="Lead" asp-action="Create">Novo Lead</a>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Email</th>
            <th>Telefone</th>
            <th>CPF</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id </td>
                <td>@item.Nome </td>
                <td>@item.Email</td>
                <td>@item.Telefone</td>
                <td>@item.CPF</td>
                <td style="white-space:nowrap">
                    <a class="btn btn-primary btn-sm" asp-controller="Lead" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                    <a class="btn btn-danger btn-sm" asp-controller="Lead" asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
